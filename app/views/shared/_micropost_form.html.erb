<%= form_for(@micropost) do |f| %>
  <div class="field">
    <%= f.text_area :content, placeholder: "Compose new post...", :rows => "2" %>
  </div>
  <%= f.submit "Post", class: "btn btn-small btn-warning" %>
<% end %>