<div id="cellar_form">
  <%= form_for(current_user.cellared_beers.build(beer_id: params[:id]), remote: true) do |f| %>
  <div><%= f.hidden_field :beer_id %></div>
  <%= f.submit "Add to Cellar", class: "btn btn-small btn-warning" %>
  <% end %>
</div>
